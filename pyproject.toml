[project]
name = "chatbot-rag-kanilla"
version = "0.1.0"
description = "RAG chatbot with LanceDB and FastAPI"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn",
    "lancedb",
    "pydantic-ai>=1.35.0",
]
